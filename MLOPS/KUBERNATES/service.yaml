apiVersion: v1
kind: Service

metadata:
  name: weather-app-service

spec:
  type: NodePort

  selector:
    app: weather-app

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007